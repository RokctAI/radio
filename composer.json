{
  "name": "rokctapp_composer",
  "version": "1.0.0",
  "package_name": "radio",
  "description": "Configuration list of active SDKs for a radio station app",
  "sdks": [
    {
      "name": "radio_sdk",
      "enabled": true,
      "source": "git",
      "git": "https://github.com/RokctAI/agent",
      "path": "../agent/radio/dart",
      "ref": "main",
      "sha256": "8250d154072120cf0672c0447d0b24b44359cff1cbdd33af8317249401f2ec25",
      "_sha256_comment": "SHA-256 of radio/dart/install.py at agent@798de2da; required by the composer's unpinned-installer gate. Update when upstream install.py changes.",
      "home_sdk": true,
      "_comment": "A radio app's home IS the player, so this SDK provides the entry page. Declares StationsTable at schema version 17; one row for a single-station app, many for a multi-station one."
    },
    {
      "name": "base_sdk",
      "enabled": true,
      "source": "git",
      "git": "https://github.com/RokctAI/core",
      "path": "../core/base/dart",
      "ref": "main",
      "sha256": "909d60c5455993693ad4169653e961e99f683ecfa1c8f8f714f787fc2d39c740",
      "_sha256_comment": "SHA-256 of base/dart/install.py at core@573d570; required by the composer's unpinned-installer gate. Update when upstream install.py changes.",
      "_comment": "Shared kernel (replaces retired core_sdk); provides the app shell templates, AppStyle, the splash and no-connection pages, and the AppDatabase radio_sdk registers StationsTable into."
    },
    {
      "name": "comms_sdk",
      "enabled": true,
      "source": "git",
      "git": "https://github.com/RokctAI/core",
      "path": "../core/comms/dart",
      "ref": "main",
      "sha256": "02b841bed985380ade9885a4c61b150db9e6c6eb635183018ac21b02e30126fb",
      "_sha256_comment": "SHA-256 of comms/dart/install.py at core@573d570; required by the composer's unpinned-installer gate. Update when upstream install.py changes.",
      "_comment": "Push notifications. The lineage this app came from used OneSignal directly; comms_sdk replaces that."
    },
    {
      "name": "telemetry_sdk",
      "enabled": true,
      "source": "git",
      "git": "https://github.com/RokctAI/core",
      "path": "../core/telemetry/dart",
      "ref": "main",
      "sha256": "f4ebf13b33b28b668332407790e2aa6f5a0d79283a845200188b77f7f8035ba6",
      "_comment": "Listenership telemetry. The telemetry CLIENT lives in base_sdk (TelemetryClient.track / logError, gateway cmds tenant.api.track_event / tenant.api.log_frontend_error); telemetry_sdk (core/telemetry/dart) ships no client of its own — it composes on top and injects delivery policy through the TelemetryClient.configure transport seam. Radio listening is accumulated locally in the app's Drift database (radio_sdk's listen_daily_totals aggregates) and flushed on cold start as one radio_listen_daily summary per station-day, so listener figures are daily aggregates, not live tune-in counts; the frappe side keeps src/telemetry with the api_error_log and client_usage_event doctypes. Pin = sha256 of core telemetry/dart/install.py as bootstrapped, same pattern as desktop_sdk's entry."
    },
    {
      "name": "promotions_sdk",
      "enabled": false,
      "source": "git",
      "git": "https://github.com/RokctAI/commerce",
      "path": "../commerce/promotions/dart",
      "ref": "main",
      "sha256": "0477b9efef1e82b3391ab92d638881a4113a0a11b1debd3a64715f606007264d",
      "_sha256_comment": "SHA-256 of promotions/dart/install.py at commerce@71394ca8; required by the composer's unpinned-installer gate. Update when upstream install.py changes.",
      "_comment": "Ad mediation. Disabled by default: the station this was built for runs with ads off (showADS false), so enable per app rather than per template."
    },
    {
      "name": "desktop_sdk",
      "enabled": true,
      "source": "git",
      "git": "https://github.com/RokctAI/hardware",
      "path": "../hardware/desktop/dart",
      "ref": "main",
      "sha256": "d9adf68c4c9da75ad3e7b8164253f09acb82ed3a68595687d5f138f99caf1057",
      "_comment": "TEMPORARY (desktop testing convenience, revert candidate): added to every app profile so all composed apps pull desktop support and can be viewed/tested on desktop. Pin = sha256 of hardware desktop/dart/install.py at main head 84aa6ed, same value as supacharge.json's entry."
    },
    {
      "name": "hms_sdk",
      "enabled": true,
      "source": "git",
      "git": "https://github.com/RokctAI/hardware",
      "path": "../hardware/hms/dart",
      "ref": "main",
      "sha256": "91468daf800bb0a348019c491605f25ad2d37f44682fd61211fd471a60b2d57b",
      "_comment": "Huawei Mobile Services support, built like desktop_sdk (SDK module, not productFlavors). Dart-side: DeviceServices ({gms, hms, neither} detection, cached, fail-open to gms) and an order-30 boot hook that, ONLY on GMS-free HMS-capable devices, boots Push Kit and registers the HMS push token through the same api.user.register_device_token gateway cmd the FCM token uses (provider 'hms'). No installs - the BUILD_HMS-gated AGConnect Gradle wiring lives statically in core's android template, default-off. Strict no-op in every plain GMS build."
    }
  ]
}
